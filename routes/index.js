const { authRoutes } = require('./auth-route');
const { userRoutes } = require('./user-route');
const { postRoutes } = require('./post-route');

module.exports = { authRoutes, userRoutes, postRoutes };
